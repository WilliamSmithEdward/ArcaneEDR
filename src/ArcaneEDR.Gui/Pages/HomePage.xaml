<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ArcaneEDR_Gui.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ArcaneEDR_Gui.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="28,18,28,28" Spacing="18">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="14">
                    <Image Width="48" Height="48" Source="ms-appx:///Assets/AppLogo.png" />
                    <StackPanel>
                        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Overview" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Arcane EDR local console" />
                    </StackPanel>
                </StackPanel>
                <Button
                    Grid.Column="1"
                    Click="Refresh_Click"
                    ToolTipService.ToolTip="Refresh overview">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="16" Glyph="&#xE72C;" />
                        <TextBlock Text="Refresh" />
                    </StackPanel>
                </Button>
            </Grid>

            <Grid ColumnSpacing="12" RowSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6">
                    <StackPanel Spacing="4">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Service" />
                        <TextBlock x:Name="ServiceStateText" FontSize="22" FontWeight="SemiBold" Text="Unknown" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6">
                    <StackPanel Spacing="4">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Polls" />
                        <TextBlock x:Name="PollCountText" FontSize="22" FontWeight="SemiBold" Text="0" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6">
                    <StackPanel Spacing="4">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Alerts" />
                        <TextBlock x:Name="AlertCountText" FontSize="22" FontWeight="SemiBold" Text="0" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="3" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6">
                    <StackPanel Spacing="4">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Failures" />
                        <TextBlock x:Name="FailureCountText" FontSize="22" FontWeight="SemiBold" Text="0" />
                    </StackPanel>
                </Border>
            </Grid>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6">
                    <StackPanel Spacing="8">
                        <TextBlock FontWeight="SemiBold" Text="Health" />
                        <TextBlock x:Name="HealthText" TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6">
                    <StackPanel Spacing="8">
                        <TextBlock FontWeight="SemiBold" Text="Paths" />
                        <TextBlock x:Name="PathText" TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </Border>
            </Grid>

            <TextBox
                x:Name="OutputText"
                AcceptsReturn="True"
                FontFamily="Consolas"
                Header="Latest validation"
                IsReadOnly="True"
                MinHeight="220"
                TextWrapping="NoWrap" />
        </StackPanel>
    </ScrollViewer>
</Page>
