<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ArcaneEDR_Gui.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:ArcaneEDR_Gui.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ArcaneEDR_Gui.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer
        controls:PageScrollBehavior.UseReliableWheel="True"
        Style="{StaticResource PageScrollViewerStyle}">
        <StackPanel Padding="{StaticResource PagePadding}" Spacing="18">
            <Grid ColumnSpacing="18" RowSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image
                    Width="76"
                    Height="76"
                    VerticalAlignment="Center"
                    Source="ms-appx:///Assets/AppLogo.png" />
                <StackPanel
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Spacing="4">
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Overview" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="Arcane EDR local console"
                        TextWrapping="Wrap" />
                </StackPanel>
                <Button
                    Grid.Column="2"
                    VerticalAlignment="Top"
                    AutomationProperties.Name="Refresh overview"
                    Click="Refresh_Click"
                    ToolTipService.ToolTip="Refresh overview">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="16" Glyph="&#xE72C;" />
                        <TextBlock Text="Refresh" />
                    </StackPanel>
                </Button>
                <Button
                    Grid.Column="3"
                    VerticalAlignment="Top"
                    AutomationProperties.Name="Overview help"
                    Click="Help_Click"
                    ToolTipService.ToolTip="Explain overview recommendations">
                    <FontIcon FontSize="16" Glyph="&#xE897;" />
                </Button>
            </Grid>

            <Grid ColumnSpacing="12" RowSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource SectionCardStyle}">
                    <StackPanel Spacing="4">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Service" />
                        <TextBlock x:Name="ServiceStateText" FontSize="22" FontWeight="SemiBold" Text="Unknown" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}">
                    <StackPanel Spacing="4">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Polls" />
                        <TextBlock x:Name="PollCountText" FontSize="22" FontWeight="SemiBold" Text="0" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource SectionCardStyle}">
                    <StackPanel Spacing="4">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Alerts" />
                        <TextBlock x:Name="AlertCountText" FontSize="22" FontWeight="SemiBold" Text="0" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="3" Style="{StaticResource SectionCardStyle}">
                    <StackPanel Spacing="4">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Failures" />
                        <TextBlock x:Name="FailureCountText" FontSize="22" FontWeight="SemiBold" Text="0" />
                    </StackPanel>
                </Border>
            </Grid>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Style="{StaticResource SectionCardStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock FontWeight="SemiBold" Text="Review priorities" />
                        <ItemsControl x:Name="RecommendationsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border
                                        Margin="0,0,0,8"
                                        Padding="10"
                                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6">
                                        <StackPanel Spacing="4">
                                            <TextBlock
                                                FontSize="12"
                                                FontWeight="SemiBold"
                                                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                Text="{Binding Priority}" />
                                            <TextBlock
                                                FontWeight="SemiBold"
                                                Text="{Binding Title}"
                                                TextWrapping="Wrap" />
                                            <TextBlock
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Text="{Binding Detail}"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock FontWeight="SemiBold" Text="Current signal picture" />
                        <TextBlock x:Name="SignalSummaryText" TextWrapping="Wrap" />
                        <TextBlock
                            Margin="0,8,0,0"
                            FontWeight="SemiBold"
                            Text="Health timestamps" />
                        <TextBlock x:Name="HealthText" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </Grid>

            <StackPanel Spacing="8">
                <TextBlock x:Name="ValidationHeadingText" FontWeight="SemiBold" Text="Configuration validation" />
                <Border MinHeight="170" Style="{StaticResource ConsolePanelStyle}">
                    <TextBlock x:Name="OutputText" Style="{StaticResource ConsoleTextStyle}" />
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
