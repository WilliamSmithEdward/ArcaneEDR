<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ArcaneEDR_Gui.Pages.PolicyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:ArcaneEDR_Gui.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="{StaticResource PagePadding}" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Policy" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Unified scoped policy" />
            </StackPanel>
            <Button
                Grid.Column="1"
                AutomationProperties.Name="Inspect policy"
                Click="Inspect_Click"
                ToolTipService.ToolTip="Inspect policy">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="16" Glyph="&#xE721;" />
                    <TextBlock Text="Inspect" />
                </StackPanel>
            </Button>
            <Button
                Grid.Column="2"
                AutomationProperties.Name="Preview sample policy match"
                Click="Preview_Click"
                ToolTipService.ToolTip="Preview sample policy match">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="16" Glyph="&#xE8A5;" />
                    <TextBlock Text="Preview" />
                </StackPanel>
            </Button>
            <Button
                Grid.Column="3"
                AutomationProperties.Name="Open policy file"
                Click="OpenPolicy_Click"
                ToolTipService.ToolTip="Open policy file">
                <FontIcon FontSize="16" Glyph="&#xE8A5;" />
            </Button>
            <Button
                Grid.Column="4"
                AutomationProperties.Name="Policy help"
                Click="Help_Click"
                ToolTipService.ToolTip="Explain policy safety model">
                <FontIcon FontSize="16" Glyph="&#xE897;" />
            </Button>
        </Grid>

        <InfoBar
            x:Name="PolicyInfo"
            Grid.Row="1"
            IsOpen="True"
            Severity="Informational"
            Title="JSONL evidence"
            Message="Policy decisions tune alerting and response gates while local alert evidence remains in JSONL." />

        <TabView x:Name="PolicyTabs" Grid.Row="2">
            <TabViewItem Header="Entries" IsClosable="False">
                <Grid RowSpacing="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="220" />
                    </Grid.RowDefinitions>

                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Policy file" />
                                <TextBlock x:Name="PolicyPathText" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Rules" />
                                <TextBlock x:Name="PolicyRuleCountText" FontSize="20" FontWeight="SemiBold" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Allowlist countries" />
                                <TextBlock x:Name="PolicyCountryCountText" FontSize="20" FontWeight="SemiBold" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="3" Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Status" />
                                <TextBlock x:Name="PolicyLoadStatusText" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid Grid.Row="1" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="190" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ComboBox
                            x:Name="PolicyScopeFilterBox"
                            Header="Scope"
                            SelectedIndex="0"
                            SelectionChanged="PolicyFilter_Changed" />
                        <TextBox
                            x:Name="PolicySearchBox"
                            Grid.Column="1"
                            Header="Search"
                            PlaceholderText="id, action, owner, reason, match, country, process"
                            TextChanged="PolicySearchBox_TextChanged" />
                    </Grid>

                    <ListView
                        x:Name="PolicyEntriesList"
                        Grid.Row="2"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.HorizontalScrollMode="Disabled"
                        ScrollViewer.IsHorizontalRailEnabled="False"
                        ScrollViewer.IsHorizontalScrollChainingEnabled="False"
                        ScrollViewer.IsScrollInertiaEnabled="False"
                        ScrollViewer.IsVerticalScrollChainingEnabled="False"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        ScrollViewer.VerticalScrollMode="Enabled"
                        ScrollViewer.ZoomMode="Disabled"
                        SelectionChanged="PolicyEntriesList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8,7" ColumnSpacing="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="270" />
                                        <ColumnDefinition Width="84" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Scope}" TextTrimming="CharacterEllipsis" />
                                    <StackPanel Grid.Column="1" Spacing="2">
                                        <TextBlock FontWeight="SemiBold" Text="{Binding Id}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding Owner}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding EnabledDisplay}" TextTrimming="CharacterEllipsis" />
                                    <StackPanel Grid.Column="3" Spacing="2">
                                        <TextBlock Text="{Binding Action}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding Score}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="4" Spacing="2">
                                        <TextBlock Text="{Binding Reason}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding MatchSummary}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Border Grid.Row="3" Style="{StaticResource ConsolePanelStyle}">
                        <Grid RowSpacing="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock x:Name="SelectedPolicyTitleText" FontWeight="SemiBold" Text="Select a policy entry" />
                            <TextBlock
                                x:Name="SelectedPolicyDetailText"
                                Grid.Row="1"
                                Style="{StaticResource ConsoleTextStyle}" />
                        </Grid>
                    </Border>
                </Grid>
            </TabViewItem>

            <TabViewItem Header="Inspect Output" IsClosable="False">
                <ScrollViewer
                    controls:PageScrollBehavior.UseReliableWheel="True"
                    Style="{StaticResource PageScrollViewerStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock FontWeight="SemiBold" Text="Policy output" />
                        <Border MinHeight="520" Style="{StaticResource ConsolePanelStyle}">
                            <TextBlock x:Name="PolicyOutputText" Style="{StaticResource ConsoleTextStyle}" />
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabViewItem>

            <TabViewItem Header="Raw JSON" IsClosable="False">
                <ScrollViewer
                    controls:PageScrollBehavior.UseReliableWheel="True"
                    Style="{StaticResource PageScrollViewerStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock FontWeight="SemiBold" Text="Current unified policy file" />
                        <Border MinHeight="520" Style="{StaticResource ConsolePanelStyle}">
                            <TextBlock x:Name="PolicyRawText" Style="{StaticResource ConsoleTextStyle}" />
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabViewItem>
        </TabView>
    </Grid>
</Page>
